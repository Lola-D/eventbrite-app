  <div class="jumbotron">
    <div class="container">
      <h1 class="display-3">Modifie ton profil</h1>
    </div>
  </div>

<div class="card ml-5 mr-5 p-5 mt-5">
  <%= form_for(@user, as: :user, url: user_path(@user), method: :patch, html: { class: "edit_user", id: "edit_user.id" }) do |f|  %>

      <div class="field">
        <%= f.label :first_name, "Ton prénom" ,class: "col-md-4 control-label"%>
        <%= f.text_field :first_name, class:"col-md-6 form-control mb-4" %>
      </div>

      <div class="field">
        <%= f.label :last_name, "Ton nom", class: "col-md-4 control-label" %>
        <%= f.text_field :last_name, class:"col-md-6 form-control mb-4" %>
      </div>

      <div class="field">
        <%= f.label :description, "Ta description", class: "col-md-4 control-label" %>
        <%= f.text_area :description, class:"col-md-6 form-control mb-4" %>
      </div>

      <div class="actions">
        <%= f.submit "Editer", class:"btn btn-primary mb-4" %>
      </div>

      <%= link_to "Retour au profil", user_path(@user) %>
  <% end %>
</div>
